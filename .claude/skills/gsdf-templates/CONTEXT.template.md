# Phase {{PHASE}} Context: {{NAME}}

## Decisions

One bullet per decision the user made. Concrete values, not intentions —
"Gain range -60..+12 dB, default 0, skew 3.0", not "sensible gain range".

## Rejected

What was considered and turned down, each with the reason. This is what stops the
planner re-proposing it.

## Open questions for the planner

Things the user explicitly left to Claude. Name them so the planner knows they are
its call and not an oversight.
