---
phase: 02-gain-stage
plan: 01
type: execute
wave: 1
requirements: [REQ-02, REQ-03]
autonomous: true
---

<objective>
Add an automatable gain parameter to the APVTS.

Purpose: every later plan in this phase and the whole of phase 3 binds to this parameter.
Output: a `gain` parameter visible to the host, plus a unit test pinning its range.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@Source/PluginProcessor.h
@Source/ParamIDs.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the gain parameter ID</name>
  <files>Source/ParamIDs.h</files>
  <action>Add `static constexpr auto gain = "gain";` to the ParamIDs namespace. Do not
  reuse or rename any existing ID — parameter IDs are an ABI.</action>
  <verify>grep -q 'gain' Source/ParamIDs.h</verify>
  <done>ParamIDs::gain exists and compiles</done>
</task>

<task type="auto">
  <name>Task 2: Add gain to the APVTS layout</name>
  <files>Source/PluginProcessor.cpp, Source/PluginProcessor.h</files>
  <action>In createParameterLayout, add an AudioParameterFloat for ParamIDs::gain with
  NormalisableRange&lt;float&gt;(-60.0f, 12.0f, 0.1f, 3.0f) and default 0.0f. Do NOT add it to
  processBlock yet — that is plan 02-02.</action>
  <verify>cmake --build build --config Release && ctest --test-dir build -R Gain</verify>
  <done>Host shows a Gain parameter, range -60..+12 dB, default 0</done>
</task>

</tasks>

<verification>
- [ ] Build passes
- [ ] ctest -R Gain passes
- [ ] Parameter appears in the standalone's parameter list
</verification>
