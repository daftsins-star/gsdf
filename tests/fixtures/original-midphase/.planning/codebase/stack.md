# Stack

- C++20, JUCE 8.0.4 via CPM, CMake 3.25+
- Test framework: Catch2 v3 through ctest
- UI: vanilla HTML/CSS/JS in `ui/`, served by vite in dev, inlined at build
- Formats built: VST3, AU (macOS), Standalone
