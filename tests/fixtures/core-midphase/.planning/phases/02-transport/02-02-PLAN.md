---
phase: 02-transport
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [Source/Voice.cpp, Source/dsp/Crossfade.h]
requirements: [REQ-05]
autonomous: true
---

<objective>
Equal-power crossfade across the loop join.
</objective>

<context>
@Source/dsp/LoopPoints.h
@Source/Voice.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Equal-power crossfade table</name>
  <files>Source/dsp/Crossfade.h</files>
  <action>Precompute sin/cos gain pairs at prepare time. Never call std::sin in the
  audio thread.</action>
  <verify>
    <automated>ctest --test-dir build -R Crossfade</automated>
    <fails_when>non-zero exit, or the summed-power assertion deviates by more than 0.01</fails_when>
  </verify>
  <done>Summed power across the fade stays within 0.01 of unity</done>
</task>

<task type="auto">
  <name>Task 2: Apply the crossfade at the loop join</name>
  <files>Source/Voice.cpp</files>
  <action>Read ahead from the loop start into the tail of the loop and mix with the
  precomputed gains. Do not allocate — the read-ahead buffer is sized in prepareToPlay.</action>
  <verify>
    <automated>ctest --test-dir build -R LoopJoin</automated>
    <fails_when>non-zero exit, or the discontinuity metric exceeds -60 dBFS</fails_when>
  </verify>
  <done>Discontinuity at the join measures below -60 dBFS on the pad test material</done>
</task>

</tasks>

<verification>
- [ ] Crossfade power test passes
- [ ] LoopJoin discontinuity test passes
</verification>
