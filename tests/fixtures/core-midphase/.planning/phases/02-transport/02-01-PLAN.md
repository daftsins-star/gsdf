---
phase: 02-transport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Source/dsp/LoopPoints.h, Source/Voice.cpp]
requirements: [REQ-04]
autonomous: true
must_haves:
  truths:
    - "A loop stays phase-locked to the host after 5 minutes of playback"
  artifacts:
    - "Source/dsp/LoopPoints.h"
---

<objective>
Store loop points as int64 sample offsets and wrap playback at them sample-accurately.
</objective>

<context>
@.planning/PROJECT.md
@Source/Voice.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: LoopPoints struct</name>
  <files>Source/dsp/LoopPoints.h</files>
  <read_first>Source/dsp/StreamBuffer.h</read_first>
  <action>Header-only struct holding start and end as int64 sample offsets plus a
  crossfadeSamples field. No JUCE dependency — it must be testable standalone.</action>
  <verify>
    <automated>ctest --test-dir build -R LoopPoints</automated>
    <fails_when>non-zero exit, or "0 tests passed" in the summary line</fails_when>
  </verify>
  <done>LoopPoints compiles standalone and its wrap arithmetic is unit tested</done>
</task>

<task type="auto">
  <name>Task 2: Sample-accurate wrap in the voice</name>
  <files>Source/Voice.cpp</files>
  <action>On reaching loop end, wrap the read position by subtracting the loop length
  rather than assigning loop start, so any overshoot inside the block is preserved.</action>
  <verify>
    <automated>ctest --test-dir build -R LoopPhase</automated>
    <fails_when>non-zero exit, or the phase drift assertion reports a non-zero delta</fails_when>
  </verify>
  <done>Phase drift after 5 simulated minutes is exactly 0 samples</done>
</task>

</tasks>

<verification>
- [ ] LoopPoints tests pass
- [ ] LoopPhase drift test passes
</verification>
